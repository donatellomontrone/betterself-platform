import { ContactPage } from "@/components/betterself-pages";

export const metadata = {
  title: "Contact",
  description:
    "Reach the BetterSelf medical team by WhatsApp or email, and see our Metro Manila service areas.",
};

export default function Contact() {
  return <ContactPage />;
}
