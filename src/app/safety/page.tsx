import { SafetyPage } from "@/components/betterself-pages";

export const metadata = {
  title: "Safety",
  description:
    "Medical screening, consent, sterile preparation, and aftercare — how BetterSelf keeps treatments safe.",
};

export default function Safety() {
  return <SafetyPage />;
}
